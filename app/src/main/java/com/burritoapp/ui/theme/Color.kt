package com.burritoapp.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales del negocio de burritos
val PrimaryOrange = Color(0xFF03A9F4)      // Naranja cálido (principal)
val PrimaryOrangeDark = Color(0xFF104B7E)  // Naranja oscuro
val SecondaryBrown = Color(0xFF684E8F)     // Café (secundario)
val AccentYellow = Color(0xFFFDB913)       // Amarillo mostaza (acentos)

// Colores de estado
val SuccessGreen = Color(0xFF4CAF50)       // Verde para pagado
val WarningOrange = Color(0xFFFF9800)      // Naranja para pendiente
val ErrorRed = Color(0xFFC73B33)           // Rojo para alertas

// Colores de fondo y superficie
val BackgroundLight = Color(0xFFFAF9F6)    // Fondo claro cálido
val SurfaceLight = Color(0xFFFFFFFF)       // Superficie blanca
val BackgroundDark = Color(0xFF1C1C1E)     // Fondo oscuro
val SurfaceDark = Color(0xFF2C2C2E)        // Superficie oscura

// Colores de texto
val TextPrimary = Color(0xFF1C1C1E)        // Texto principal oscuro
val TextSecondary = Color(0xFF757575)      // Texto secundario gris
val TextOnPrimary = Color(0xFFFFFFFF)      // Texto sobre primary
